# Optional: change dashboard PIN (default in code: 8821)
VITE_DASHBOARD_PIN=8821

# Optional: full API origin when frontend is not proxied (e.g. production split hosts)
# VITE_API_BASE=https://api.example.com
